<li class="content-item">
  <h5 class="title"><%= highlight litigation.link, @query %></h5>
  <div class="meta">
    <% if litigation.geography %>
      <div>
        <img src=<%= asset_path "flags/#{litigation.geography.iso}.svg"%> alt="">
        <%= litigation.geography.name %>
      </div>
    <% end %>
    <% if litigation.started_event.present? %>
      <div>Opened in <%= litigation.started_event.date&.year %></div>
    <% end %>
  </div>
  <div class="description">
    <%= highlight litigation.summary&.html_safe, @query %>
  </div>
</li>
