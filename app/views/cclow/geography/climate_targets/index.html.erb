<div class="container">
  <h3>Climate targets: Economy-wide</h3>
  <ul class="content-list">
    <li class="with-meta">
      <div class="meta"><img class="flag" src=<%= asset_path "flags/#{@geography.iso}.svg"%> alt="">
        This country is a member of the EU and so <span class="highlight"> EU NDC data </span> is being displayed.
      </div>
      <div class="climate-targets-section ndc-content">
        <div class="nav">
          <h5>NDC content</h5>
          <span class="counter"><%= @targets.count %> <%= @targets.count > 1 ? 'targets' : 'target' %></span>
        </div>
        <ul>
          <% @targets.each do |target| %>
            <li>
              <div>
                <%= target.description.html_safe %>
              </div>
              <div class="tags">Economy Wide | <%= I18n.t("cclow.target_types.#{target.target_type}") %> | Target year: 2050 | Base year: 1990</div>
              <div class="source">Source:
                <div class="name">European Union NDC</div>
              </div>
            </li>
          <% end %>
        </ul>
      </div>
    </li>

    <li>
      <div class="climate-targets-section">
        <div class="nav">
          <h5>Climate targets in this law</h5>
          <span class="counter"><%= @targets.count %> <%= @targets.count > 1 ? 'targets' : 'target' %></span>
        </div>
        <ul>
          <% @targets.each do |target| %>
            <li>
              <div>
                <%= target.description.html_safe %>
              </div>
              <div class="tags">Economy Wide | <%= I18n.t("cclow.target_types.#{target.target_type}") %> | Target year: 2050 | Base year: 1990</div>
              <div class="source">Source:
                <% target.legislations.each do |legislation|%>
                  <div class="name"><%= legislation.title %></div>
                  <p class="meta">
                    <span><%= I18n.t("cclow.legislation_types.#{legislation.legislation_type}") %>  |  <%= legislation.date_passed.year %></span>
                  </p>
                <% end %>
              </div>
            </li>
          <% end %>
        </ul>
      </div>
    </li>
  </ul>
  <div class="ndc-hint">
    <span>For further analysis of the NDC, see <%= @geography.name %> country profile on</span>
    <img src=<%= asset_path "cclow/climate_watch.png"%> alt="">
  </div>
</div>

